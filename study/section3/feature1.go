package main

func main() {
	/*
		GO: 애매모호한 문법 금지
		후치연산자(i++) 혀용 / 전치연산자 (++i) 금지
		증감연산 반환 값 없음 sum := i++ (X)
		포인터 허용, 포인터 연산은 비허용
		Go 파일이 컴파일 될 때 라인마다 ; 자동으로 붙인다
		두 문장을 한 라인에 표현할 때 명시적으로 ; 사용 ( Not recommended )
		if, for 사용시 ; 사용 주의
	*/
}
